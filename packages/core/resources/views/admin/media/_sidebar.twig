{% import 'admin._inc._ui' as ui %}

{{ ui.section_nav('Media', [
    {label: 'Libraries', href: route('media.libraries'), icon: 'menu', active: true},
    {label: 'New Library', href: route('media.libraries.create'), icon: 'plus', active: false}
]) }}
