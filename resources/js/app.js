import './bootstrap';
import '../../packages/core/resources/js/app.js';

// Host-specific JS goes below this line. Package behavior (tabs, permission
// checkboxes, and all packages/core admin-view helpers) lives in
// packages/core/resources/js/app.js and packages/core/resources/js/prototype.js —
// edit those in the package, not here, so `composer update` won't clobber your changes.
