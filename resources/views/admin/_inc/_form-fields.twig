{% macro input(name, label, value, type, description, placeholder, id, error_key, required=false, attrs='') %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_type = type|default('text') %}
    {% set field_value = old(field_error_key, value|default('')) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
        <input class="w-full rounded-md border bg-white px-3 py-2 text-sm shadow-sm transition placeholder:text-slate-400 focus:outline-none focus:ring-2 {% if has_error %}border-red-300 focus:border-red-500 focus:ring-red-500/20{% else %}border-slate-300 focus:border-emerald-500 focus:ring-emerald-500/20{% endif %}" id="{{ field_id }}" name="{{ name }}" type="{{ field_type }}" value="{{ field_value }}" placeholder="{{ placeholder|default('') }}" {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %} {{ attrs|raw }}>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro password(name, label, value, description, placeholder, id, error_key, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_value = old(field_error_key, value|default('')) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div data-password-field>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
        <div class="flex items-center rounded-md border bg-white shadow-sm transition focus-within:ring-2 {% if has_error %}border-red-300 focus-within:border-red-500 focus-within:ring-red-500/20{% else %}border-slate-300 focus-within:border-emerald-500 focus-within:ring-emerald-500/20{% endif %}">
            <input class="min-w-0 flex-1 border-0 bg-transparent px-3 py-2 text-sm placeholder:text-slate-400 focus:outline-none focus:ring-0" id="{{ field_id }}" name="{{ name }}" type="password" value="{{ field_value }}" placeholder="{{ placeholder|default('') }}" data-password-input {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
            <button class="inline-flex h-9 w-9 shrink-0 items-center justify-center rounded-r-md text-slate-400 hover:bg-slate-50 hover:text-slate-700 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" type="button" data-password-toggle aria-controls="{{ field_id }}" aria-label="Show password">
                <svg class="h-4 w-4" data-password-show-icon fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                <svg class="hidden h-4 w-4" data-password-hide-icon fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 3l18 18"/><path stroke-linecap="round" stroke-linejoin="round" d="M10.585 10.587a2 2 0 102.828 2.826"/><path stroke-linecap="round" stroke-linejoin="round" d="M9.88 4.24A10.95 10.95 0 0112 4c4.478 0 8.268 2.943 9.542 7a10.52 10.52 0 01-3.188 4.568M6.228 6.228A10.46 10.46 0 002.458 12c1.274 4.057 5.064 7 9.542 7 1.528 0 2.967-.343 4.248-.957"/></svg>
            </button>
        </div>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro password_scripts() %}
    <script>
        (function () {
            function initPasswordFields() {
                document.querySelectorAll('[data-password-field]').forEach(function (field) {
                    if (field.dataset.passwordReady === 'true') {
                        return;
                    }

                    var input = field.querySelector('[data-password-input]');
                    var toggle = field.querySelector('[data-password-toggle]');
                    var showIcon = field.querySelector('[data-password-show-icon]');
                    var hideIcon = field.querySelector('[data-password-hide-icon]');

                    if (!input || !toggle) {
                        return;
                    }

                    toggle.addEventListener('click', function () {
                        var isMasked = input.type === 'password';
                        input.type = isMasked ? 'text' : 'password';
                        toggle.setAttribute('aria-label', isMasked ? 'Hide password' : 'Show password');

                        if (showIcon) {
                            showIcon.classList.toggle('hidden', isMasked);
                        }

                        if (hideIcon) {
                            hideIcon.classList.toggle('hidden', !isMasked);
                        }
                    });

                    field.dataset.passwordReady = 'true';
                });
            }

            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initPasswordFields);
            } else {
                initPasswordFields();
            }
        })();
    </script>
{% endmacro %}

{% macro file(name, label, description, accept, multiple, id, error_key, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
        <label class="flex cursor-pointer flex-col items-center justify-center rounded-lg border border-dashed px-5 py-6 text-center transition {% if has_error %}border-red-300 bg-red-50 hover:border-red-400 hover:bg-red-50{% else %}border-slate-300 bg-slate-50 hover:border-emerald-400 hover:bg-emerald-50/40{% endif %}" for="{{ field_id }}">
            <span class="flex h-10 w-10 items-center justify-center rounded-full bg-white text-slate-400 shadow-sm ring-1 ring-slate-200">
                <svg class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 16V4m0 0l-4 4m4-4l4 4M4 16.5V19a1 1 0 001 1h14a1 1 0 001-1v-2.5"/></svg>
            </span>
            <span class="mt-3 text-sm font-medium text-slate-700">Choose file{% if multiple %}s{% endif %}</span>
            {% if description %}
                <span class="mt-1 max-w-sm text-xs leading-5 text-slate-500">{{ description }}</span>
            {% endif %}
            <input id="{{ field_id }}" class="sr-only" type="file" name="{{ name }}" {% if accept %}accept="{{ accept }}"{% endif %} {% if multiple %}multiple{% endif %} {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
        </label>
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro textarea(name, label, value, rows, description, placeholder, id, error_key, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_value = old(field_error_key, value|default('')) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}</label>
        <textarea class="w-full rounded-md border bg-white px-3 py-2 text-sm shadow-sm transition placeholder:text-slate-400 focus:outline-none focus:ring-2 {% if has_error %}border-red-300 focus:border-red-500 focus:ring-red-500/20{% else %}border-slate-300 focus:border-emerald-500 focus:ring-emerald-500/20{% endif %}" id="{{ field_id }}" name="{{ name }}" rows="{{ rows|default(4) }}" placeholder="{{ placeholder|default('') }}" {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>{{ field_value }}</textarea>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro select(name, label, options, value, description, id, error_key, multiple, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% if multiple %}
        {% set selected_value = old(field_error_key, value is not empty ? value : []) %}
    {% else %}
        {% set selected_value = old(field_error_key, value|default('')) %}
    {% endif %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
        <select class="w-full rounded-md border bg-white px-3 py-2 text-sm shadow-sm transition focus:outline-none focus:ring-2 {% if has_error %}border-red-300 focus:border-red-500 focus:ring-red-500/20{% else %}border-slate-300 focus:border-emerald-500 focus:ring-emerald-500/20{% endif %}" id="{{ field_id }}" name="{{ name }}" {% if multiple %}multiple{% endif %} {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
            {% if not multiple %}
                <option value="">— Select —</option>
            {% endif %}
            {% for option in options %}
                {% if option.options is defined %}
                    <optgroup label="{{ option.label }}">
                        {% for sub in option.options %}
                            {% set is_selected = multiple ? (sub.value in selected_value) : ((selected_value ~ '') == (sub.value ~ '')) %}
                            <option value="{{ sub.value }}" {% if is_selected %}selected{% endif %}>{{ sub.label }}</option>
                        {% endfor %}
                    </optgroup>
                {% else %}
                    {% set option_value = option.value|default(option.label) %}
                    {% set is_selected = multiple ? (option_value in selected_value) : ((selected_value ~ '') == (option_value ~ '')) %}
                    <option value="{{ option_value }}" {% if is_selected %}selected{% endif %}>{{ option.label }}</option>
                {% endif %}
            {% endfor %}
        </select>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro color(name, label, value, description, id, error_key, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_value = old(field_error_key, value|default('#10b981')) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div data-color-field>
        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
        <div class="flex items-center gap-3 rounded-md border bg-white px-3 py-2 shadow-sm transition focus-within:ring-2 {% if has_error %}border-red-300 focus-within:border-red-500 focus-within:ring-red-500/20{% else %}border-slate-300 focus-within:border-emerald-500 focus-within:ring-emerald-500/20{% endif %}">
            <input id="{{ field_id }}" name="{{ name }}" type="color" value="{{ field_value }}" class="h-8 w-10 shrink-0 cursor-pointer rounded border-0 bg-transparent p-0" data-color-input>
            <input name="{{ name }}_hex" type="text" value="{{ field_value }}" class="min-w-0 flex-1 border-0 bg-transparent p-0 font-mono text-sm text-slate-700 placeholder:text-slate-400 focus:outline-none focus:ring-0" data-color-hex {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
            <span class="h-6 w-6 shrink-0 rounded-full border border-slate-200 shadow-sm" style="background-color: {{ field_value }}" data-color-swatch></span>
        </div>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro color_scripts() %}
    <script>
        (function () {
            function normalizeHex(value) {
                var hex = String(value || '').trim();

                if (hex.charAt(0) !== '#') {
                    hex = '#' + hex;
                }

                if (/^#[0-9a-fA-F]{6}$/.test(hex)) {
                    return hex.toLowerCase();
                }

                return null;
            }

            function initColorFields() {
                document.querySelectorAll('[data-color-field]').forEach(function (field) {
                    if (field.dataset.colorReady === 'true') {
                        return;
                    }

                    var input = field.querySelector('[data-color-input]');
                    var hexInput = field.querySelector('[data-color-hex]');
                    var swatch = field.querySelector('[data-color-swatch]');

                    if (!input || !hexInput) {
                        return;
                    }

                    var syncFromColorPicker = function () {
                        hexInput.value = input.value;

                        if (swatch) {
                            swatch.style.backgroundColor = input.value;
                        }
                    };

                    var syncFromHexInput = function () {
                        var hex = normalizeHex(hexInput.value);

                        if (!hex) {
                            return;
                        }

                        input.value = hex;
                        hexInput.value = hex;

                        if (swatch) {
                            swatch.style.backgroundColor = hex;
                        }
                    };

                    input.addEventListener('input', syncFromColorPicker);
                    input.addEventListener('change', syncFromColorPicker);
                    hexInput.addEventListener('input', syncFromHexInput);
                    hexInput.addEventListener('change', syncFromHexInput);
                    field.dataset.colorReady = 'true';
                    syncFromHexInput();
                });
            }

            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initColorFields);
            } else {
                initColorFields();
            }

            window.initColorFields = initColorFields;
        })();
    </script>
{% endmacro %}

{% macro slider(name, label, value, min, max, step, description, id, suffix, error_key, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_value = old(field_error_key, value|default(min|default(0))) %}
    {% set field_min = min|default(0) %}
    {% set field_max = max|default(100) %}
    {% set field_step = step|default(1) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div data-range-field>
        <div class="mb-1.5 flex items-center justify-between gap-3">
            <label class="block text-sm font-medium text-slate-700" for="{{ field_id }}">{{ label }}{% if required %}<span class="text-red-500">*</span>{% endif %}</label>
            <span class="rounded-md bg-slate-100 px-2 py-1 font-mono text-xs font-medium text-slate-600" data-range-value>{{ field_value }}{{ suffix|default('') }}</span>
        </div>
        <div class="group relative pt-7">
            <output class="pointer-events-none absolute top-0 -translate-x-1/2 rounded-md bg-slate-900 px-2 py-1 font-mono text-xs font-medium text-white opacity-0 shadow-sm transition group-hover:opacity-100 group-focus-within:opacity-100 after:absolute after:left-1/2 after:top-full after:-translate-x-1/2 after:border-4 after:border-transparent after:border-t-slate-900 after:content-['']" for="{{ field_id }}" data-range-output style="left: 0;">{{ field_value }}{{ suffix|default('') }}</output>
            <input id="{{ field_id }}" name="{{ name }}" type="range" value="{{ field_value }}" min="{{ field_min }}" max="{{ field_max }}" step="{{ field_step }}" data-range-input data-range-suffix="{{ suffix|default('') }}" class="h-2 w-full cursor-pointer appearance-none rounded-full bg-slate-200 accent-emerald-600 focus:outline-none focus:ring-2 {% if has_error %}focus:ring-red-500/20{% else %}focus:ring-emerald-500/20{% endif %}" {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
        </div>
        <div class="mt-1 flex items-center justify-between font-mono text-[11px] text-slate-400">
            <span>{{ field_min }}{{ suffix|default('') }}</span>
            <span>{{ field_max }}{{ suffix|default('') }}</span>
        </div>
        {% if description %}
            <p class="mt-1 text-xs leading-5 text-slate-400">{{ description }}</p>
        {% endif %}
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro slider_scripts() %}
    <script>
        (function () {
            function initRangeSliders() {
                document.querySelectorAll('[data-range-field]').forEach(function (field) {
                    if (field.dataset.rangeReady === 'true') {
                        return;
                    }

                    var input = field.querySelector('[data-range-input]');
                    var output = field.querySelector('[data-range-output]');
                    var valueBadge = field.querySelector('[data-range-value]');

                    if (!input || !output) {
                        return;
                    }

                    var update = function () {
                        var min = Number(input.min || 0);
                        var max = Number(input.max || 100);
                        var value = Number(input.value || min);
                        var percent = max === min ? 0 : ((value - min) / (max - min)) * 100;
                        var thumbOffset = 8 - (percent * 0.16);
                        var displayValue = input.value + (input.dataset.rangeSuffix || '');

                        output.value = displayValue;
                        output.textContent = displayValue;
                        output.style.left = 'calc(' + percent + '% + ' + thumbOffset + 'px)';

                        if (valueBadge) {
                            valueBadge.textContent = displayValue;
                        }
                    };

                    input.addEventListener('input', update);
                    input.addEventListener('change', update);
                    input.addEventListener('pointermove', update);
                    window.addEventListener('resize', update);
                    field.dataset.rangeReady = 'true';
                    update();
                });
            }

            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initRangeSliders);
            } else {
                initRangeSliders();
            }

            window.initRangeSliders = initRangeSliders;
        })();
    </script>
{% endmacro %}

{% macro toggle(name, label, value, description, checked_value, id, error_key, copy_align, required=false) %}
    {% set field_id = id|default(name|replace({'[': '_', ']': '', '.': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set field_value = checked_value|default('1') %}
    {% set checked_state = old(field_error_key) is not null ? old(field_error_key) : value|default(false) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    {% set toggle_layout = copy_align|default('left') %}
    {% set text_first = toggle_layout != 'right' and toggle_layout != 'inline' %}
    {% set is_inline = toggle_layout == 'inline' %}
    {% set copy %}
        <span class="{% if is_inline %}shrink-0 text-left{% else %}min-w-0{% endif %} {% if not text_first and not is_inline %}text-right{% endif %}">
            <span class="block text-sm font-medium text-slate-700">{{ label }}</span>
            {% if description %}
                <span class="mt-0.5 block text-xs leading-5 text-slate-500">{{ description }}</span>
            {% endif %}
        </span>
    {% endset %}
    {% set control %}
        <span class="shrink-0">
            <input type="hidden" name="{{ name }}" value="0">
            <input id="{{ field_id }}" class="peer sr-only" type="checkbox" name="{{ name }}" value="{{ field_value }}" {% if checked_state %}checked{% endif %} {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
            <span class="relative block h-6 w-11 rounded-full bg-slate-300 transition after:absolute after:left-1 after:top-1 after:h-4 after:w-4 after:rounded-full after:bg-white after:shadow after:transition after:content-[''] peer-checked:bg-emerald-600 peer-checked:after:translate-x-5 peer-focus-visible:outline peer-focus-visible:outline-2 peer-focus-visible:outline-offset-2 peer-focus-visible:outline-emerald-500"></span>
        </span>
    {% endset %}
    <div>
        <label class="flex cursor-pointer items-center gap-4 rounded-md border px-4 py-3 transition {% if is_inline %}justify-start{% else %}justify-between{% endif %} {% if has_error %}border-red-300 bg-red-50 hover:border-red-400{% else %}border-slate-200 bg-slate-50 hover:border-emerald-300 hover:bg-emerald-50/30{% endif %}" for="{{ field_id }}">
            {% if text_first %}
                {{ copy }}
                {{ control }}
            {% else %}
                {{ control }}
                {{ copy }}
            {% endif %}
        </label>
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}

{% macro checkbox_card(name, value, label, description, checked, id, error_key, checked_value, required=false) %}
    {% set field_id = id|default((name ~ '_' ~ value)|replace({'[': '_', ']': '', '.': '_', ' ': '_'})) %}
    {% set field_error_key = error_key|default(name|replace({'[]': '', '[': '.', ']': ''})) %}
    {% set old_values = old(field_error_key) %}
    {% set checked_state = old_values is not null ? value in old_values : checked|default(false) %}
    {% set field_value = checked_value|default(value) %}
    {% set has_error = errors is defined and errors.has(field_error_key) %}
    <div>
        <label class="group flex cursor-pointer items-start gap-3 rounded-lg border bg-white p-4 shadow-sm transition hover:border-emerald-300 hover:bg-emerald-50/30 has-[:checked]:border-emerald-400 has-[:checked]:bg-emerald-50/60 {% if has_error %}border-red-300{% else %}border-slate-200{% endif %}" for="{{ field_id }}">
            <input id="{{ field_id }}" type="checkbox" name="{{ name }}" value="{{ field_value }}" class="mt-0.5 rounded border-slate-300 text-emerald-600 focus:ring-emerald-500" {% if checked_state %}checked{% endif %} {% if has_error %}aria-invalid="true" aria-describedby="{{ field_id }}_error"{% endif %}>
            <span class="min-w-0">
                <span class="block text-sm font-medium text-slate-800">{{ label }}</span>
                {% if description %}
                    <span class="mt-0.5 block text-xs leading-5 text-slate-500">{{ description }}</span>
                {% endif %}
            </span>
        </label>
        {% if has_error %}
            <p id="{{ field_id }}_error" class="mt-1 text-xs text-red-600">{{ errors.first(field_error_key) }}</p>
        {% endif %}
    </div>
{% endmacro %}
