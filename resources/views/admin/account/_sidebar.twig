{% import 'admin._inc._ui' as ui %}

{{ ui.section_nav('Account', [
    {label: 'Details', href: route('account.details'), icon: 'menu', active: active == 'details'},
    {label: 'Password', href: route('account.password'), icon: 'key', active: active == 'password'},
    {label: 'API Tokens', href: route('account.tokens.index'), icon: 'key', active: active == 'tokens'},
    {label: 'Settings', href: route('account.settings'), icon: 'menu', active: active == 'settings'}
]) }}
