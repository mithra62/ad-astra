{{ include('admin._inc._header', {title: 'Create Category Group'}) }}
<main>
    <div class="container-fluid">
        <!-- Breadcrumb start -->
        <div class="row m-1">
            <div class="col-12 ">
                <h5>Category Groups</h5>
                <div class="col-6 ">
                    <ul class="app-line-breadcrumbs mb-3">
                        <li class="">
                            <a class="f-s-14 f-w-500" href="{{ route('dashboard') }}">
                                <span>
                                    <i class="ph-duotone  ph-table f-s-16"></i> Dashboard
                                </span>
                            </a>
                        </li>
                        <li class="">
                            <a class="f-s-14 f-w-500" href="{{ route('categories.groups') }}">Category Groups</a>
                        </li>
                        <li>
                            <a class="f-s-14 f-w-500" href="{{ route('categories.groups.show', group) }}">{{ group.name }}</a>
                        </li>
                        <li class="active">
                            <a class="f-s-14 f-w-500" href="{{ route('categories.groups.edit', group) }}">Edit Category Group</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
        <!-- Breadcrumb end -->
        {{ include('admin._inc._message') }}

        <!-- Custom Styles start -->
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex flex-column gap-2">
                    <h5>Edit Category Group</h5>
                </div>
                <div class="card-body">
                    <form method="POST" action="{{ route('categories.groups.update', group) }}" class="row g-3 needs-validation" novalidate>
                        {{ csrf_field() }}
                        <input type="hidden" name="id" value="{{ group.id }}" />
                        {{ method_field('PUT') }}
                        <div class="col-md-12">
                            <label class="form-label" for="name">Name</label>
                            <input class="form-control" id="name" required type="text" value="{{ old('name', group.name) }}" name="name">
                            {% if errors.has('name') %}
                                <p class="text-danger">{{ errors.first('name') }}</p>
                            {% endif %}
                        </div>
                        <div class="col-md-12">
                            <label class="form-label" for="slug">Slug</label>
                            <input class="form-control" id="slug" required type="text" value="{{ old('slug', group.slug) }}" name="slug">
                            {% if errors.has('slug') %}
                                <p class="text-danger">{{ errors.first('slug') }}</p>
                            {% endif %}
                        </div>



                        {% if field_groups.count >= 1 %}

                            <div class="mb-12">
                                <label class="form-label" for="category_groups">Field Groups</label>
                                {% for fg in field_groups %}
                                    <div class="form-check d-flex">
                                        <input class="form-check-input mg-2"
                                               id="library.{{ fg.slug }}"
                                               type="checkbox"
                                               name="field_groups[]"
                                               value="{{ fg.id }}"
                                            {% if old('field_groups') %}
                                                {% for key, field in old('field_groups') %}
                                                    {% if old('field_groups.' ~ key) == fg.id %}checked{% endif %}
                                                {% endfor %}

                                            {% else %}
                                                {% for key, field in group.field_groups %}
                                                    {% if field.id == fg.id %}checked{% endif %}
                                                {% endfor %}
                                            {% endif %}
                                        >
                                        <label class="form-check-label ms-2" for="library.{{ fg.slug }}">
                                            {{ fg.name }}
                                        </label>
                                    </div>
                                {% endfor %}
                                {% if errors.has('category_groups') %}
                                    <p class="text-danger">{{ errors.first('category_groups') }}</p>
                                {% endif %}
                            </div>
                        {% else %}

                        {% endif %}

                        <div class="col-12">
                            <button class="btn btn-primary float-end" type="submit">Edit Category Group</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <!-- Custom Styles end -->
    </div>
</main>

{{ include('admin._inc._footer') }}
