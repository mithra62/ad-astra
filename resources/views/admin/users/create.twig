{% extends 'admin._inc._layout' %}

{% set page_title = 'Create User' %}
{% set page_heading = 'Create User' %}
{% set active_nav = 'users' %}
{% set active_user_section = 'create' %}
{% set has_secondary_sidebar = true %}

{% block breadcrumbs %}
    <nav class="mt-0.5 flex items-center gap-1.5 text-xs text-slate-500 sm:text-sm">
        <a class="hover:text-slate-800" href="home.twig"><svg class="inline-block h-3 w-3 align-middle" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 12l9-9 9 9M5 10v9a1 1 0 001 1h4v-5h4v5h4a1 1 0 001-1v-9"/></svg> Dashboard</a>
        <svg class="h-3 w-3 text-slate-300" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
        <a class="hover:text-slate-800" href="users.twig">Users</a>
        <svg class="h-3 w-3 text-slate-300" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
        <span class="text-slate-800">Create User</span>
    </nav>
{% endblock %}

{% block secondary_sidebar %}
    {{ include('admin.users._sidebar') }}
{% endblock %}

{% block content %}
    <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div>
            <h1 class="text-xl font-semibold tracking-tight text-slate-900 sm:text-2xl">Create User</h1>
            <p class="mt-1 text-sm text-slate-500">Add a new user and assign one or more roles.</p>
        </div>
        <a href="users.twig" class="inline-flex items-center gap-1.5 self-start rounded-md border border-slate-300 bg-white px-3 py-2 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50 sm:self-auto"><svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/></svg>Back to Users</a>
    </div>

    <div class="rounded-lg border border-amber-200 bg-amber-50 p-4">
        <div class="flex items-start gap-3">
            <svg class="mt-0.5 h-5 w-5 shrink-0 text-amber-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/></svg>
            <div><p class="text-sm font-semibold text-amber-800">Invitation email will be sent after save.</p><p class="mt-1 text-sm text-amber-700">Users can set their own password from the secure invite link.</p></div>
        </div>
    </div>

    <form method="POST" action="{{ route('users.store') }}" class="flex flex-col gap-5 lg:grid lg:grid-cols-[1fr_18rem] lg:items-start">
        {{ csrf_field() }}
        <div class="flex min-w-0 flex-col gap-4">
            <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">
                <div class="border-b border-slate-200 px-5 py-3"><h5 class="text-sm font-semibold text-slate-700">Profile</h5></div>
                <div class="space-y-4 px-5 py-5">
                    <div>
                        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="email">Email Address <span class="text-red-500">*</span></label>
                        <input value="{{ old('email') }}" class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm shadow-sm placeholder:text-slate-400 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" id="email" name="email" type="email" placeholder="jane@example.com">
                        <p class="mt-1 text-xs text-slate-400">This is used for login and notifications.</p>
                        {% if errors.has('email') %}<p id="email-error" class="mt-1 text-xs text-red-600">{{ errors.first('email') }}</p>{% endif %}
                    </div>
                    <div>
                        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="username">Username</label>
                        <div class="flex rounded-md shadow-sm">
                            <span class="inline-flex items-center rounded-l-md border border-r-0 border-slate-300 bg-slate-50 px-3 text-xs text-slate-500">@</span>
                            <input value="{{ old('name') }}" class="w-full rounded-r-md border border-slate-300 px-3 py-2 text-sm placeholder:text-slate-400 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" id="name" name="name" type="text" placeholder="jane.smith">
                        </div>
                        {% if errors.has('name') %}<p id="name-error" class="mt-1 text-xs text-red-600">{{ errors.first('name') }}</p>{% endif %}
                    </div>
                </div>
            </div>




            {% if schema.fieldLayout %}
                {% for tab in schema.fieldLayout.tabs %}
                <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">
                    <div class="border-b border-slate-200 px-5 py-3"><h5 class="text-sm font-semibold text-slate-700">{{ tab.name }}</h5></div>
                    <div class="space-y-4 px-5 py-5">

                    {% for element in tab.elements %}
                        {% set field = element.field %}

                        <div>
                            <label class="mb-1.5 block text-sm font-medium text-slate-700" for="field_{{ field.handle }}">{{ field.label }}<span class="text-red-500">*</span></label>
                            {{ field.render({
                                id: 'field_' ~ field.handle,
                                class: 'w-full rounded-md border border-slate-300 px-3 py-2 text-sm shadow-sm placeholder:text-slate-400 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20'
                            })|raw }}
                            {% if errors.has('fields.' ~ field.handle) %}
                                <p id="email-error" class="mt-1 text-xs text-red-600">{{ errors.first('fields.' ~ field.handle) }}</p>
                            {% endif %}
                        </div>
                    {% endfor %}
                    </div>
                </div>
                {% endfor %}

            {% endif %}



            <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">
                <div class="border-b border-slate-200 px-5 py-3"><h5 class="text-sm font-semibold text-slate-700">Password</h5></div>
                <div class="space-y-4 px-5 py-5">
                    <div>
                        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="email">Password <span class="text-red-500">*</span></label>
                        <input value="" class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm shadow-sm placeholder:text-slate-400 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" id="password" name="password" type="password">
                        <p class="mt-1 text-xs text-slate-400">This is used for login and notifications.</p>
                        {% if errors.has('password') %}<p id="password-error" class="mt-1 text-xs text-red-600">{{ errors.first('password') }}</p>{% endif %}
                    </div>
                    <div>
                        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="username">Username</label>
                        <div class="flex rounded-md shadow-sm">
                            <span class="inline-flex items-center rounded-l-md border border-r-0 border-slate-300 bg-slate-50 px-3 text-xs text-slate-500">@</span>
                            <input value="" class="w-full rounded-r-md border border-slate-300 px-3 py-2 text-sm placeholder:text-slate-400 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" id="password_confirmation" name="password_confirmation" type="password">
                        </div>
                        {% if errors.has('password_confirmation') %}<p id="password_confirmation-error" class="mt-1 text-xs text-red-600">{{ errors.first('password_confirmation') }}</p>{% endif %}
                    </div>
                </div>
            </div>


            <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">
                <div class="border-b border-slate-200 px-5 py-3"><h5 class="text-sm font-semibold text-slate-700">Roles</h5></div>
                <div class="grid grid-cols-1 gap-3 px-5 py-5 sm:grid-cols-2 xl:grid-cols-3">
                    {% for role in roles %}
                        <label class="flex cursor-pointer items-start gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm transition hover:border-emerald-300 hover:bg-emerald-50/30">
                            <input type="checkbox" name="roles[]" value="{{ role.name }}"
                                   class="mt-0.5 rounded border-slate-300 text-emerald-600 focus:ring-emerald-500"
                                {% if old('roles') %}
                                    {% for key, field in old('roles') %}
                                        {% if old('roles.' ~ key) == role.name %}checked{% endif %}
                                    {% endfor %}
                                {% endif %}
                            >
                            <span>
                            <span class="block text-sm font-medium text-slate-800">{{ role.name }}</span>
                            <span class="mt-0.5 block text-xs leading-5 text-slate-500">{{ role.description }} fdsa</span>
                        </span>
                        </label>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="flex min-w-0 flex-col gap-4">
            <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">
                <div class="border-b border-slate-200 px-5 py-3"><h5 class="text-sm font-semibold text-slate-700">Account</h5></div>
                <div class="space-y-4 px-5 py-5">
                    <div>
                        <label class="mb-1.5 block text-sm font-medium text-slate-700" for="status">Status</label>
                        <select class="w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-sm shadow-sm focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500/20" id="status" name="status">
                            <option value="active" selected>Active</option>
                            <option value="invited">Invited</option>
                            <option value="suspended">Suspended</option>
                        </select>
                    </div>
                    <label class="flex cursor-pointer items-center gap-2.5 text-sm text-slate-700">
                        <input type="checkbox" name="send_invite" checked class="rounded border-slate-300 text-emerald-600 focus:ring-emerald-500">
                        Send invite email
                    </label>
                    <label class="flex cursor-pointer items-center gap-2.5 text-sm text-slate-700">
                        <input type="checkbox" name="require_2fa" class="rounded border-slate-300 text-emerald-600 focus:ring-emerald-500">
                        Require two-factor authentication
                    </label>
                </div>
                <div class="space-y-2 border-t border-slate-100 px-5 py-4">
                    <button type="submit" class="w-full rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2">Create User</button>
                    <a href="users.twig" class="block w-full rounded-md border border-slate-300 bg-white px-4 py-2 text-center text-sm font-medium text-slate-600 shadow-sm hover:bg-slate-50">Cancel</a>
                </div>
            </div>
        </div>
    </form>
{% endblock %}
