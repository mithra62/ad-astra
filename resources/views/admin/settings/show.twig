{% extends 'admin._inc._layout' %}
{% import 'admin._inc._form-fields' as forms %}
{% import 'admin._inc._ui' as ui %}

{% set page_title = 'Settings' %}
{% set page_heading = 'Settings' %}
{% set active_nav = 'settings' %}
{% set active_secondary_nav = 'system' %}
{% set has_secondary_sidebar = true %}

{% block breadcrumbs %}
    {{ ui.breadcrumbs([{label: 'Dashboard', href: route('dashboard'), icon: 'home'}, {label: 'Settings'}]) }}
{% endblock %}

{% block secondary_sidebar %}
    {{ include('admin.settings._sidebar', {all_domains: domains, current_handle: domain.handle}) }}
{% endblock %}

{% block content %}
    <div id="unsaved-bar" class="unsaved-bar items-center justify-between rounded-lg border border-amber-200 bg-amber-50 px-4 py-2">
        <span class="flex items-center gap-2 text-sm text-amber-800">
            <svg class="h-4 w-4 shrink-0 text-amber-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
            </svg>
            You have unsaved changes.
        </span>
        <button type="submit" form="general-create-form" class="rounded-md bg-emerald-600 px-3 py-1 text-xs font-semibold text-white hover:bg-emerald-700">Save Now</button>
    </div>

    <div class="flex w-full min-w-0 flex-col gap-5">
        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <h1 class="text-xl font-semibold tracking-tight text-content sm:text-2xl">Settings</h1>
                <p class="mt-1 text-sm text-muted">Create a reusable status, field, label, or option for content workflows.</p>
            </div>
        </div>

        {% if grouped_fields|length > 0 %}
            <form id="general-create-form" method="POST" action="{{ route('settings.update', domain.handle) }}" class="flex w-full min-w-0 flex-col gap-5">
                {{ csrf_field() }}
                {{ method_field('PUT') }}






                {% for groupName, fields in grouped_fields %}
                <div class="overflow-hidden rounded-lg border border-edge bg-surface shadow-sm">
                    <div class="border-b border-edge px-5 py-3">
                        <h5 class="text-sm font-semibold text-content">{{ groupName ? groupName : domain.name }}</h5>
                    </div>
                    <div class="space-y-5 px-5 py-5">

                    {% for field in fields %}
                        {% set val = field_values[field.handle] is defined ? field_values[field.handle] : (field.default ?? '') %}
                        {% set field_description = field.instructions %}

                        {% if field.user_overridable %}
                            <p class="text-xs text-muted">
                                <span class="inline-flex items-center rounded-full bg-blue-50 px-2 py-0.5 text-xs font-medium text-blue-700 ring-1 ring-inset ring-blue-700/10">user overridable</span>
                            </p>
                        {% endif %}

                        {% if field.type == 'boolean' %}
                            {{ forms.toggle(field.handle, field.label, val, field_description) }}
                        {% elseif field.type == 'textarea' %}
                            {{ forms.textarea(field.handle, field.label, val, 4, field_description) }}
                        {% elseif field.type == 'select' or (field.options is defined and field.options|length) %}
                            {{ forms.select(field.handle, field.label, field.options|default([]), val, field_description) }}
                        {% elseif field.type == 'integer' %}
                            {{ forms.input(field.handle, field.label, val, 'number', field_description, '', '', field.handle, false, 'step="1"') }}
                        {% elseif field.type == 'float' %}
                            {{ forms.input(field.handle, field.label, val, 'number', field_description, '', '', field.handle, false, 'step="any"') }}
                        {% else %}
                            {{ forms.input(field.handle, field.label, val, field.type == 'email' ? 'email' : 'text', field_description) }}
                        {% endif %}
                    {% endfor %}
                    </div>
                </div>
                {% endfor %}





                <div class="flex flex-col-reverse gap-3 sm:flex-row sm:items-center sm:justify-end">
                    <button type="submit" class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2">Save Settings</button>
                </div>
            </form>
        {% endif %}
    </div>
{% endblock %}
