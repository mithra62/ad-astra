<textarea class="{{ class ?? 'form-control' }}" id="{{ id }}" name="fields[{{ field.handle }}]"
          placeholder="{{ field.instructions }}" rows="3">{{ old('fields.' ~ field.handle, value) }}</textarea>
