<input value="{{ old('fields.' ~ field.handle, value) }}" class="{{ class ?? 'form-control' }}" id="{{ id }}"
       type="number" name="fields[{{ field.handle }}]" placeholder="{{ field.instructions }}">
